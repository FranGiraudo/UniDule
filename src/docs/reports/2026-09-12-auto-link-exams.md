# Reporte de Implementación: Auto-vinculación de Exámenes Viejos

- **Objetivo**: Evitar que el usuario tenga que crear sus parciales viejos desde cero, vinculando las tareas preexistentes (desvinculadas) a la tabla de evaluaciones.
- **Archivos modificados**:
  - `src/features/tasks/lib/api.ts` (función `linkExistingExams` agregada).
  - `src/pages/Tasks.tsx` (se ejecuta la función silenciosamente al entrar).
- **Estado**: Listo.
